let fib = fn (n) => {
  if (n < 2) {
    n
  } else {
    fib(n - 1) + fib(n - 2)
  }
};

let run_fib = fn (n) => {
    let f = fib(n);
    print("Fibonacci(" + n + ") = " + f)
};

let _ = run_fib(10);
let _ = run_fib(20);
run_fib(24)
